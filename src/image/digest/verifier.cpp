#include "image/digest/verifier.h"
/**
 * @brief 写入数据到验证器
 * @param p 要验证的数据向量
 * @return int 返回0表示成功
 * @details 将数据写入验证器进行后续验证处理
 */
int hashVerifier::write( std::vector<char> p){
    return 0;
}
/**
 * @brief 验证数据完整性
 * @return bool 返回true表示验证通过
 * @details 比较计算出的摘要与预期摘要是否匹配
 */
bool hashVerifier::Verified(){
    // auto newd=New
    // return digest->digest=
    return true;
}
