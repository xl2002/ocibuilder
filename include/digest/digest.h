#if !defined(GITHUB_DIGEST_H)
#define GITHUB_DIGEST_H
#include <string>
#include <vector>
using std::string;
using std::vector;

class Digest{
    public:
    string digest;

};

#endif // GITHUB_DIGEST_H
