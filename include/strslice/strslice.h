#if !defined(STRSLICE_STRSLICE_H)
#define STRSLICE_STRSLICE_H
#include <string>
#include <vector>
#include <memory>
#include <map>
struct StrSlice{
    std::vector<std::string> strlice;

};

#endif // STRSLICE_STRSLICE_H
