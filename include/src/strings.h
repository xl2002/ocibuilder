#if !defined(SRC_STRINGS_H)
#define SRC_STRINGS_H
#include <string>
#include <iostream>
#include <vector>
#include <memory>
#include "io/io.h"
        
bool HasPrefix(const std::string& data,const std::string& prefix);
#endif // SRC_STRINGS_H